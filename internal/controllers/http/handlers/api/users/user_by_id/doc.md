# Пакет users/user_by_id

## Назначение

Получение пользователя по ID.

## Маршрут

`GET /api/users/{id}` → `Handler(u)`

## Ответы

- `200` — JSON с данными пользователя
- `400` — некорректный ID
- `401` — не авторизован
- `404` — пользователь не найден
