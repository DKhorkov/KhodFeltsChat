# Пакет users/users

## Назначение

Список пользователей с фильтрацией по username и пагинацией.

## Маршрут

`GET /api/users` → `Handler(u)`

## Query-параметры

`username` — фильтр по имени, `limit`, `offset` — пагинация.

## Ответы

- `200` — JSON-массив пользователей
- `401` — не авторизован
