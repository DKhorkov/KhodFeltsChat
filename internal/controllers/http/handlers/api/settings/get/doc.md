# Пакет settings/get

## Назначение

Получение настроек текущего пользователя.

## Маршрут

`GET /api/users/me/settings` → `Handler(s)`

## Ответы

- `200` — JSON с настройками
- `401` — не авторизован
- `404` — настройки не найдены
