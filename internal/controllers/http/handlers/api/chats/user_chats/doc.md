# Пакет chats/user_chats

## Назначение

Список чатов текущего пользователя с пагинацией.

## Маршрут

`GET /api/chats` → `Handler(u)`

## Query-параметры

`limit`, `offset` — пагинация (опционально).

## Ответы

- `200` — JSON-массив чатов
- `401` — не авторизован
