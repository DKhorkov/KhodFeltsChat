# Пакет handlers/web/common

## Назначение

Shared-утилиты для веб-обработчиков.

## Функции

| Функция | Описание |
|---------|----------|
| `RenderError(w, code, message)` | Рендерит HTML-страницу ошибки с кодом и сообщением. Шаблон `error.html` + `navbar.html` парсится один раз (`sync.Once`). |
